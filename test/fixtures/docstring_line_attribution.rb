# frozen_string_literal: true

# Fixture for docstring line attribution tests
class DocstringLineAttribution
  # Processes the input data
  # in several consecutive steps — including validation
  # Note: this can be slow on large inputs
  # • first step of the pipeline
  # @return [void]
  def process; end
end
