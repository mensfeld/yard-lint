# frozen_string_literal: true

# Fixture for the Documentation/UnderfilledLines end-to-end test.

# This documentation paragraph wraps far too early and
# wastes most of the available width on each of its lines.
def underfilled
end

# Processes the given record and returns a normalized hash that downstream code relies on.
def well_filled
end

# Validates the input.
# Normalizes the casing.
# Persists the record.
def semantic_breaks
end

# Supported formats:
# - json
# - yaml
def with_list
end

# This single documentation line is deliberately written to exceed the configured maximum length of one hundred twenty characters total.
def too_long
end
